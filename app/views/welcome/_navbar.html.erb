<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <div class="d-flex flex-grow-1">
            <span class="w-100 d-lg-none d-block">
                <!-- hidden spacer to center brand on mobile --></span>
           <%= link_to "Notes Cabinet", root_path, :class => "navbar-brand" %>
            <div class="w-100 text-right">
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#myNavbar7">
                    <span class="navbar-toggler-icon"></span>
                </button>
            </div>
        </div>

        <div class="collapse navbar-collapse flex-grow-1 text-right" id="myNavbar7">
            <ul class="navbar-nav ms-auto flex-nowrap">

                <% if user_signed_in?%>
                    <li class="nav-item"><%= link_to "Create Notes", new_doc_path, :class => "nav-link" %></li>
                    <li class="nav-item"><%= link_to "My Notes", docs_path, :class => "nav-link" %></li>
                    <li class="nav-item"><%= link_to "Sign Out", destroy_user_session_path, method: :delete, data: { turbo: "false" }, class: "nav-link" %></li>
                <% else %>
                    <li class="nav-item"><a href="#" class="nav-link">About</a></li>
                    <li class="nav-item"><%= link_to "Sign In", new_user_session_path, :class => "nav-link" %></li>
                <% end %>

            </ul>
        </div>
    </div>
</nav>